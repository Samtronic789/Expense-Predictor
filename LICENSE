
When someone clicks **Sample CSV** in GitHub, it will open `sample.csv` in the browser.

---

# 🛠 Step 3: Initialize Git & Push to GitHub

1. Go to [GitHub](https://github.com/) → create a **new repository** (name: `expense-predictor`).
2. On your computer, open a terminal inside the `expense-predictor/` folder:

```bash
git init
git add .
git commit -m "Initial commit - Personal Expense Predictor"
git branch -M main
git remote add origin https://github.com/YOUR-USERNAME/expense-predictor.git
git push -u origin main
